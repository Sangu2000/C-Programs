#include<stdio.h>
int main(){
    int criteria = 60 ;
    if(criteria<60 ) {
        printf("You can't apply for company"); }
    else if(criteria=60 ) {
        printf("we will see your portfolio");
    }
    else{
        printf("You can apply for company");
    }
        return 0;
} 